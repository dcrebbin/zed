#!/usr/bin/env bash

set -euo pipefail

profile="${1:-debug}"
log_path="target/${profile}/cursor-tab-debug.log"

if [[ ! -f "${log_path}" ]]; then
  echo "No Cursor Tab debug log found at ${log_path}. Run that build and reproduce the issue first." >&2
  exit 1
fi

cat "${log_path}"
